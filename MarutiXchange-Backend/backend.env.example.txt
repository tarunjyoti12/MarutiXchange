# ============================================================
# MarutiXchange BACKEND — .env.example  (SAFE TO COMMIT)
# Goes in the ROOT of your MarutiXchange-Backend folder.
#
# Copy this to ".env" and fill in real values:
#     cp .env.example .env
# The .env file stays on your machine and is never pushed.
# ============================================================

# ---- Database ----
DB_HOST=localhost
DB_USERNAME=root
DB_PASSWORD=put_your_db_password_here

# ---- JWT (token signing secret) ----
# MUST be the same value for every service, min 32 characters.
JWT_SECRET=generate_a_long_random_256_bit_secret_here

# ---- Eureka (service discovery login) ----
EUREKA_HOST=localhost
EUREKA_USERNAME=admin
EUREKA_PASSWORD=put_a_strong_password_here

# ---- Email (user-service OTP) ----
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password

# ---- Kafka ----
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ---- Redis ----
REDIS_HOST=localhost
REDIS_PORT=6379
