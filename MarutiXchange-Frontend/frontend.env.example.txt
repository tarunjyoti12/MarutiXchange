# ============================================================
# MarutiXchange FRONTEND — .env.example  (SAFE TO COMMIT)
# Goes NEXT TO package.json inside your frontend app folder
# (the "Frontend" folder that has package.json and src/).
#
# Copy this to ".env" and fill in real values:
#     cp .env.example .env
# ============================================================

# Base URL of your backend API gateway
VITE_API_BASE_URL=http://localhost:8064

# IMPORTANT about Groq:
# Anything starting with VITE_ is visible to anyone in the browser.
# So do NOT put your Groq API key here. Instead, call Groq from a
# backend service and let the frontend call your backend.
# (Until you move it, keep the key ONLY in your local .env, never in git.)
# VITE_GROQ_API_KEY=do_not_commit_real_key
